# 1. pip install uv
# 2. uv init .
# 3. uv add fastmcp (to install fastmcp using uv instead of pip)
# 4. uv run fastmcp dev inspector main.py (to test the fast mcp)
# 5. uv run python server.py (To run the fastmcp server)
# 6. Create categories json to fource claude to use structure category
# 7. Create REST API using fast api for travel expense and test in app.py file
# 8. then convert this into MCP server in server.py file
# 9. uv sync
# 10. uv add fastapi uvicorn pydantic (to use your existing uv project)